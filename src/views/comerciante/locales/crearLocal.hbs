<div class="container mt-4">
    <h3> Crear un local</h3>

    <form method="POST" action="/comerciante/crearLocal" class="mt-4" name="form" id="form">

        <nav class="border-0">
            <div class="nav nav-tabs border-0" id="nav-tab" role="tablist">
                <a class="nav-item nav-link text-success active" id="nav-home-tab" data-toggle="tab" href="#nav-home"
                    role="tab" aria-controls="nav-home" aria-selected="true"><i class="fas fa-store"></i> Datos del
                    local</a>
                <a class="nav-item nav-link text-success" id="nav-profile-tab" data-toggle="tab" href="#nav-profile"
                    role="tab" aria-controls="nav-profile" aria-selected="false"><i class="fas fa-apple-alt"></i>
                    Productos</a>
            </div>
        </nav>
        <div class="tab-content" id="nav-tabContent">

            <div class="tab-pane fade show active" id="nav-home" role="tabpanel" aria-labelledby="nav-home">

                <div class="card shadow border-top-0">
                    <div class="container pt-3">
                        <div class="form-group">
                            <label for="name">¿Cómo se llama el local?</label>
                            <input class="form-control" type="text" placeholder="Ejemplo: Fruver Don jacinto"
                                name="name" id="name">
                        </div>
                        <div class="form-group">
                            <label for="descripcion">Descripcion al público</label>
                            <textarea name="descripcion" id="descripcion" class="form-control"
                                placeholder="Ejemplo: Las mejores frutas y verduras de la ciudad" form="forsm"> </textarea>
                        </div>
                        <div class="form-group">
                            <label for="tipoLocal">¿Qué tipo de local es?</label>
                            <select name="tipoLocal" id="tipoLocal" class="form-control">

                                <option value="0">Minorista</option>
                                <option value="1">Mayorista</option>

                            </select>
                        </div>
                    </div>
                </div>

            </div>


            <div class="tab-pane fade" id="nav-profile" role="tabpanel" aria-labelledby="nav-profile">
                <div class="card shadow border-top-0">
                    <div class="container pt-3">
                        <h5>Selecciona los productos que vendes</h5>
                        <small class="text-muted">Las variantes de tu producto se añadirán después</small>
                        <div class="productos row mt-2">
                            {{#each rowsProductos}}
                            <div class="form-control border-0 col-sm-12 col-lg-3">
                                <label><input   type="checkbox"  value="{{pkIdProducto}}" name="productos"> {{nombreProducto}}</label>
                            </div>
                            {{/each}}
                        </div>
                        
                    </div>
                </div>
            </div>
        </div>
        <button type="submit" class="btn btn-success mt-3"><i class="fas fa-check"></i> Crear local</button>
    </form>
</div>


{{>navegacionComerciante}}